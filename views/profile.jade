extend layout
block content
    h2 Profile Page #{username}
    if ownPage == true
        form(action="/upload" method="post" enctype="multipart/form-data" role="form")
            .form-group
                label(for="fieldImage") Image:
                input(type="file" id="fieldImage" name="image" accept="image/*")
            button(type="submit" class="btn btn-default") Submit
    if loggedIn == true && ownPage == false
        a(href="/follow/#{username}") Follow Me
    ul.list-unstyled
        for image in images
            li
                a(href="#{image.fullSizePath}")
                    img(src="#{image.thumbPath}")
                p
                    | Uploaded by
                    a(href="/profile/#{image.user_name}") #{image.user_name}